# Active experiment outputs

The redesigned driver writes checkpoint fragments and summaries below
`main_experiment/`. Generated outputs are ignored by Git.

All results from the earlier pipeline are preserved under
`archive/legacy_pipeline/experiment_results/`.
